server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://121.196.105.48:3306/magicmusic?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  #  mapper-locations: classpath:mapping\SongMapper.xml
  mapper-locations: classpath*:mapping/*.xml
  type-aliases-package: com.g06.springboot.entity

logging:
  level:
    com:
      example:
        mapper : debug